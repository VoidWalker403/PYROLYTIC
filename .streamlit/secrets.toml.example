# Copy to secrets.toml and replace the placeholder with output from python auth.py.
# The placeholder intentionally cannot authenticate anyone.
[auth.users]
admin = "REPLACE_WITH_GENERATED_PASSWORD_HASH"
